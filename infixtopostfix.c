#include<stdio.h>
#include<ctype.h>
typedef struct conversion
{
char[30];
int top;
}stack;
void push
char pop
void convert
int priority






























if(in[])




post[j++]=opr;
opr=pop(&s1);
}
push(&s1,opr);
push(&s1,in[i]);
}
else

